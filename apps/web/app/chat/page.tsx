import { TabPlaceholder } from '@/components/shell/AppShell';

export default function ChatPage() {
  return (
    <TabPlaceholder title="Chat" owningStory="Epic 6 — Social & Messaging">
      Direct messages and partner-finding are not part of Epic 4. This tab
      exists so the bottom bar keeps its final shape while the map ships.
    </TabPlaceholder>
  );
}
